from plptest import *

TestConfig = c = {}

test = Test(
  name = 'body_detection',
  commands = [
    Shell('clean', 'make clean'),
    Shell('build', 'make all export NO_BRIDGE=1'),
    Shell('run',   'make run platform=gvsoc')
  ],
  timeout=1000000
)

c['tests'] = [ test ]
